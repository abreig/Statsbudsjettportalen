# Database connection (required)
ConnectionStrings__DefaultConnection=Host=localhost;Database=statsbudsjett;Username=statsbudsjett;Password=localdev

# JWT secret for mock auth (min 32 chars, change in production)
JwtSettings__Secret=poc-secret-key-not-for-production-use-only-minimum-32-chars

# CORS allowed origins (comma-separated, not needed when SPA is served from backend)
Cors__AllowedOrigins__0=http://localhost:5173

# Enable Swagger UI in non-Development environments
EnableSwagger=true

# Azure-specific (set as App Settings in Azure Portal)
# ASPNETCORE_ENVIRONMENT=Production
# WEBSITES_PORT=8080
